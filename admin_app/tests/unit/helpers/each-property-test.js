import {
  eachProperty
} from '../../../helpers/each-property';
import { module, test } from 'qunit';

module('EachPropertyHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = eachProperty(42);
  assert.ok(result);
});
