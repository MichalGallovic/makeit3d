import {
  objectToRow
} from '../../../helpers/object-to-row';
import { module, test } from 'qunit';

module('ObjectToRowHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = objectToRow(42);
  assert.ok(result);
});
